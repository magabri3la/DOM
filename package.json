{
  "homepage": "https://magabri3la.github.io/lazy-forfox/",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\"",
    "pre-commit": "pretty-quick --staged",
    "install:husky": "husky install",
    "predeploy" : "snowpack build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "husky": "7",
    "prettier": "^2.0.5",
    "pretty-quick": "^3.1.0",
    "snowpack": "^3.0.11",
    "tailwindcss": "^2.0.2"
  },
  "dependencies": {
    "gh-pages": "^4.0.0",
    "hyperscript": "^2.0.2"
  }
}
